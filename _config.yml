title: TopoCluster
# logo: /assets/img/logo.png
description: TopoCluster is a localized data structure for simplicial meshes, and it provides efficient computation of the connectivity of the mesh elements with a low memory footprint. 
show_downloads: true
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
